<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:icon="@drawable/ic_ordem_preto_24dp"
        android:title="@string/ordem_por"
        app:showAsAction="ifRoom">
        <menu>
            <group
                android:id="@+id/menu_sort_by"
                android:checkableBehavior="single">
                <item
                    android:id="@+id/ordem_mais_popular"
                    android:checked="true"
                    android:title="@string/ordem_por_mais_popular" />
                <item
                    android:id="@+id/ordem_mais_votados"
                    android:title="@string/ordem_por_mais_votados" />

                <item
                    android:id="@+id/ordem_favoritos"
                    android:title="@string/ordem_por_favoritos" />
            </group>
        </menu>
    </item>
</menu>